APPLICATION HEALTH — FINAL PRE-IMPLEMENTATION AUDIT
====================================================

IMPORTANT: DO NOT MODIFY ANY CODE YET.

This is a READ-ONLY architecture + dependency + Kubernetes-behavior audit.

The goal is to completely understand the current Applications Health implementation and determine exactly what must change for the requested Applications Health column + diagnosis popover.

I do NOT want an iterative "discover something → modify → discover another thing → modify again" process.

Perform ONE COMPLETE AUDIT FIRST.

Use BOTH:
1. The entire relevant codebase
2. The available live Kubernetes cluster

Live cluster access is READ-ONLY for this audit.
Do not create, modify, delete, restart, scale, patch, or otherwise mutate any cluster resource.

At the end, produce ONE definitive audit report.
DO NOT implement anything until the audit is complete.

====================================================
PART 1 — ESTABLISH CURRENT BASELINE
====================================================

Before anything else:

1. Record git status.
2. Record the current git diff.
3. Confirm the working tree state.
4. Identify the exact branch/commit being audited.
5. Do not modify files.

Then identify the exact current Applications Health rendering path:

Application row
→ Health column
→ health calculation
→ resource fetching
→ status calculation
→ existing tooltip/popover/icon components

Trace the complete runtime path with exact files, functions and relevant lines.

====================================================
PART 2 — COMPLETE RESOURCE INVENTORY
====================================================

This is mandatory.

Do NOT assume the resource list from the previous audit is complete.

Build a COMPLETE inventory of every Kubernetes resource kind that could participate in an Application.

Determine all of the following independently:

A. Resource kinds included in defaultApiResources.

B. Resource kinds added by plugin registration / registerProjectApiResource.

C. Resource kinds referenced through ResourceClasses or special-case logic.

D. Resource kinds actually fetched by useProjectItems/useProjectResources/useKubeLists for Applications.

E. Resource kinds actually returned in the items array.

F. Resource kinds rendered in the Applications Resources tab.

G. Resource kinds present in the live cluster for the Applications being inspected.

H. Resource kinds referenced anywhere in the existing Applications health implementation.

I. Resource kinds that are NOT fetched today but appear relevant to Application health.

J. Resource kinds that are fetched today but currently have no meaningful health evaluation.

Reconcile all of these lists.

Explicitly identify:

- supported and fetched kinds
- supported but not fetched kinds
- fetched but not health-evaluated kinds
- displayed but not health-evaluated kinds
- referenced/special-case kinds
- plugin-provided kinds
- unknown/unclassified kinds
- kinds completely absent from the current implementation

Do not stop at the 19 kinds found in the previous audit.

====================================================
PART 3 — PRODUCE THE DEFINITIVE RESOURCE CLASSIFICATION
====================================================

For EVERY discovered resource kind, determine which category it belongs to:

1. HEALTH-BEARING
2. SUPPORTING / CONNECTIVITY
3. INFORMATIONAL
4. UNKNOWN / UNCLASSIFIED
5. NOT CURRENTLY SUPPORTED / NOT FETCHED

Do not choose these categories based on assumption.

For every classification explain WHY.

Example:

Deployment
→ Health-bearing
→ Because desired/available replicas and rollout conditions directly describe workload health.

Service
→ Supporting/connectivity or informational
→ Explain exactly why and whether endpoint availability should affect Application health.

ConfigMap
→ Informational
→ Existence alone does not establish workload health.

etc.

IMPORTANT:

The classification must be based on:
- Kubernetes semantics
- existing Headlamp behavior
- current Application UI behavior
- actual resource relationships
- live cluster evidence where available

If the correct classification is uncertain, explicitly mark it uncertain and explain what evidence is missing.

====================================================
PART 4 — DEFINE EXACT HEALTH SIGNALS PER KIND
====================================================

For EVERY health-bearing kind, identify the exact Kubernetes fields/conditions that determine:

HEALTHY
DEGRADED / PROGRESSING
UNHEALTHY / FAILED
UNKNOWN

Do not use vague descriptions such as "check status".

Specify the actual fields/conditions.

Examples of the level of detail required:

Deployment:
- desired replicas
- available replicas
- ready replicas
- updated replicas
- conditions
- unavailable replicas
- Progressing
- Available
- ReplicaFailure

Pod:
- phase
- Ready condition
- containerReady conditions
- container waiting reasons
- CrashLoopBackOff
- ImagePullBackOff
- terminated reason/exit state
- Pending
- Failed

Job:
- Complete
- Failed
- active
- conditions

PVC:
- Bound
- Pending
- Lost

StatefulSet:
- desired/current/ready replicas
- update status
- conditions if applicable

DaemonSet:
- desired/current/ready/available numbers

ReplicaSet:
- desired/current/ready replicas

And similarly for every other resource kind discovered.

Do NOT assume that all resource kinds should use the same generic getStatus() semantics.

====================================================
PART 5 — VERIFY CURRENT getStatus() BEHAVIOR
====================================================

Inspect getStatus() completely.

Determine:

- every branch
- every resource kind special-cased
- what happens to unsupported kinds
- what happens when status fields are missing
- what happens when a resource cannot actually be evaluated
- whether the function defaults/falls through to success
- whether "not evaluated" can currently appear as Healthy

Document concrete examples.

IMPORTANT:

Do NOT modify getStatus().

We already know it has external consumers including Resource Map/graph functionality.

Treat it as READ-ONLY.

====================================================
PART 6 — DETERMINE WHAT "UNKNOWN" SHOULD MEAN
====================================================

Define UNKNOWN precisely for Applications.

Investigate all realistic sources:

- RBAC/API errors
- resource unavailable
- incomplete object/status
- unsupported resource kind
- missing required status fields
- transient fetch state
- failed evaluation
- resources intentionally not health-bearing

Separate:

CHECKING
from
UNKNOWN
from
NO RESOURCES
from
INFORMATIONAL

These must NOT be conflated.

Explicitly answer:

"When exactly should an Application show UNKNOWN?"

And:

"When should something remain INFORMATIONAL instead of becoming UNKNOWN?"

Do not introduce a new shared Kubernetes status enum merely to implement this.

====================================================
PART 7 — LOADING / CHECKING / NO RESOURCES
====================================================

Trace the asynchronous data flow completely.

Determine:

- when resource fetching begins
- how loading is represented
- how completion is represented
- how errors are represented
- whether loading is per Application or shared
- whether items can temporarily be empty
- whether the current UI displays "No Resources" before fetching finishes
- whether rendering is synchronous/asynchronous
- whether each Application row calculates independently

Compare this with the existing Cluster-tab "Checking" behavior shown in the provided screenshots.

Determine the safest way for Applications Health to display:

CHECKING
while fetching is incomplete,

and:

NO RESOURCES
only after fetching completed successfully and there are genuinely zero applicable resources.

Do not implement yet.

====================================================
PART 8 — PARENT/CHILD RESOURCE RELATIONSHIPS
====================================================

Inspect actual ownerReferences and Application resource relationships.

Verify:

Deployment → ReplicaSet → Pod
StatefulSet → Pod
DaemonSet → Pod
ReplicaSet → Pod
Job → Pod
and any other relevant relationships discovered.

Use both code inspection and live cluster examples where available.

Determine whether the current Applications Health calculation double/triple counts parent and child resources.

Example:

Deployment unhealthy
+ ReplicaSet unhealthy
+ Pod unhealthy

must NOT automatically become three independent failures if they represent one underlying workload problem.

Determine the correct Application-level aggregation model.

Do NOT change getStatus() to solve this.

The deduplication, if required, must remain Applications-specific.

====================================================
PART 9 — LIVE CLUSTER VERIFICATION
====================================================

Use the live cluster extensively but READ-ONLY.

For representative Applications/resources, inspect actual Kubernetes objects.

Verify as many of these as actually exist:

- healthy Deployment
- degraded Deployment
- failed Deployment
- healthy Pod
- Pending Pod
- Failed Pod
- CrashLoopBackOff
- ImagePullBackOff
- healthy StatefulSet
- degraded StatefulSet
- healthy DaemonSet
- degraded DaemonSet
- healthy ReplicaSet
- failed ReplicaSet
- successful Job
- failed Job
- running Job
- Bound PVC
- Pending PVC
- Lost PVC if available
- Service
- Service with/without endpoints
- Ingress
- HPA
- ConfigMap
- Secret
- NetworkPolicy
- Role
- RoleBinding
- ResourceQuota
- LimitRange
- Endpoint/EndpointSlice
- any plugin/custom resources actually present

For each discovered kind record:

- actual object existence
- actual status fields
- actual conditions
- how the current code interprets it
- whether that interpretation is correct
- whether it should influence Application health

If a requested scenario does not exist in the live cluster, say so explicitly.
Do NOT manufacture runtime evidence.

====================================================
PART 10 — CURRENT VS DESIRED HEALTH MODEL
====================================================

Produce a table for EVERY relevant resource kind:

| Kind | Exists in code? | Fetched? | Displayed? | Current health evaluation | Health-bearing? | Supporting? | Informational? | Unknown possible? | Exact Kubernetes signal | Current problem | Proposed handling |

Do not leave rows out.

Then produce the definitive Application status aggregation rules.

For example:

CHECKING
→ data not yet completely available

NO RESOURCES
→ successful fetch completed + zero applicable resources

HEALTHY
→ all health-bearing resources evaluated healthy

DEGRADED
→ at least one health-bearing resource is progressing/not-ready without definitive failure

UNHEALTHY
→ at least one health-bearing resource has a definitive failure

UNKNOWN
→ health cannot be reliably determined

But DO NOT blindly use these examples.
Derive and justify the final rules from the audit.

Explicitly define precedence.

For example:

UNHEALTHY > DEGRADED > UNKNOWN > HEALTHY

or whatever is actually correct.

Explain why.

====================================================
PART 11 — CURRENT UI / POPOVER AUDIT
====================================================

Inspect the existing Applications Health cell.

Determine:

- what is rendered today
- which component is used
- how icon/color/status is selected
- whether tooltip/popover already exists
- whether any existing popover pattern can be reused
- whether Cluster-tab "Checking" can be reused without modifying Cluster code
- whether row-local state is needed
- whether existing per-row state patterns already exist

The requested popover should eventually explain the health decision.

Determine what information is available without additional API calls.

Do NOT implement yet.

====================================================
PART 12 — API / FETCHING IMPACT
====================================================

Determine whether the requested health calculation can use the already-fetched items.

Verify whether:

- new API calls are required
- existing items already contain all required fields
- useKubeLists errors are currently dropped
- loading state is currently available
- resource status/conditions are already present

If additional data is required, identify exactly what and why.

Prefer existing fetched data.

Do not modify shared fetching hooks.

====================================================
PART 13 — SHARED DEPENDENCY SAFETY
====================================================

For every function proposed to be called or changed, perform a complete usage search.

Especially:

- getStatus
- getGraphNodeStatus
- useKubeLists
- useProjectItems
- getResourcesHealth
- getHealthIcon
- StatusLabel
- defaultApiResources
- registerProjectApiResource

For every one determine:

- all callers
- whether callers exist outside Applications
- whether it is public/shared
- whether changing it could affect other features

Classify:

READ/CALL SAFE
ADDITIVE EXTENSION ONLY
DO NOT MODIFY

No shared implementation/signature/semantic changes should be proposed unless absolutely unavoidable.

====================================================
PART 14 — EXACT FILE SCOPE
====================================================

After the complete audit, produce three lists:

A. MUST NOT TOUCH
B. MAY TOUCH, ADDITIVE ONLY
C. LIKELY FILES TO CHANGE FOR APPLICATION HEALTH

For every proposed file explain exactly why it needs to change.

Anything unrelated to the Applications Health column/popover must remain out of scope.

====================================================
PART 15 — TEST MATRIX BEFORE IMPLEMENTATION
====================================================

Create a complete test matrix covering:

- Checking
- No Resources
- Healthy
- Degraded
- Unhealthy
- Unknown
- mixed resource states
- parent/child duplication
- RBAC/API errors
- unsupported resource kind
- missing status
- empty resources
- loading transition
- multiple Applications rendering independently
- popover open/close isolation
- existing health regression
- existing Resource Map behavior
- existing Advanced Search behavior

Also identify exact existing tests that protect shared functions.

Do not modify tests yet.

====================================================
PART 16 — FINAL GO / NO-GO DECISION
====================================================

At the end give ONE explicit recommendation:

GO — implementation can begin safely

OR

NO-GO — something still needs verification

If GO, provide the exact implementation plan.

The implementation plan must contain:

1. Exact files to change
2. Exact functions/components to add or extend
3. Exact resource classification
4. Exact health rules
5. Exact status precedence
6. Exact Unknown definition
7. Exact Checking/No Resources behavior
8. Exact parent/child deduplication strategy
9. Exact popover content
10. Exact tests to add/update
11. Explicit list of shared functions that will remain untouched

====================================================
ABSOLUTE RULES
====================================================

DO NOT IMPLEMENT ANYTHING DURING THIS AUDIT.

DO NOT:
- edit source files
- edit tests
- refactor
- rename functions
- change shared signatures
- change shared status unions
- modify Resource Map
- modify Advanced Search
- modify Cluster-tab code
- modify plugin APIs
- modify unrelated UI
- modify the live cluster

READ CODE.
TRACE CALLERS.
INSPECT TYPES.
INSPECT ALL RESOURCE REGISTRATIONS.
INSPECT ALL FETCH PATHS.
INSPECT LIVE KUBERNETES OBJECTS.
RECONCILE EVERYTHING.

The objective is to finish the entire dependency + resource + Kubernetes semantics + loading-state + UI-impact analysis in ONE PASS so that after this report we can make an informed implementation decision without repeatedly rediscovering dependencies.

FINAL OUTPUT MUST BE A SINGLE COMPLETE AUDIT REPORT.

Do not start implementation until this audit is finished and the final GO/NO-GO decision is produced.
