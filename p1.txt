/CAVEMAN

I want ONE focused implementation pass ONLY for the APPLICATION HEALTH POPOVER.

Do NOT start another broad audit.
Do NOT repeatedly re-investigate the health engine.
Do NOT create loop-after-loop verification.
Read the existing implementation once, understand it, implement the required popover correction, run the relevant tests/typecheck/lint, and STOP with a concise final report.

IMPORTANT HARD CONSTRAINT — SHARED CODE SAFETY
================================================
Shared code may be READ/FETCHED for understanding, but MUST NOT be modified.

ABSOLUTELY DO NOT MODIFY:
- getStatus
- getPodStatus
- getGraphNodeStatus
- useKubeLists
- useProjectItems
- registerProjectApiResource
- defaultApiResources
- ClusterStatusPopover
- resourceMap / graph-related shared files
- any existing shared hook/function signature
- any shared Kubernetes/resource infrastructure

This is an Applications-only feature.
If something is missing, solve it inside the Application Health implementation/components instead of changing shared code.

The existing implementation already created:
- health/
  - types.ts
  - resourceKinds.ts
  - healthApiResources.ts
  - resourceHealth.ts
  - dedupe.ts
  - evaluateApplicationHealth.ts
  - useApplicationHealth.ts
  - ApplicationHealthPopover.tsx
  - evaluateApplicationHealth.test.ts
- Application-local integration in:
  - ProjectList.tsx
  - ProjectDetails.tsx
  - ProjectResourcesTab.tsx
  - ProjectDetails.test.tsx

Preserve the current health-engine architecture unless a very small Applications-local change is required for the popover.

================================================
CURRENT PROBLEM
================================================

The current popover is NOT acceptable.

From the current UI:
- some health badges open a popover while other health badges do not
- the popover mostly shows only problem resources
- healthy resources do not provide useful information
- there is no proper application-level health summary
- there is no proper resource count/status breakdown
- Deployment status/count is not shown
- Pod status/count is not shown
- StatefulSet/DaemonSet/ReplicaSet information is not properly shown
- Job/CronJob/PVC information is incomplete
- supporting resources are not represented
- informational resources are not represented
- the popover does not explain WHY the application is Healthy/Degraded/Unhealthy/Unknown
- the current popover is therefore not the contract we designed

DO NOT simply add an onClick to the existing StatusLabel and call this done.
The popover needs to become a useful Application Health summary.

================================================
REQUIRED POPOVER BEHAVIOUR
================================================

Every Application Health badge that represents the Application Health state MUST have the same clickable/popover behaviour.

This applies whether the application is:
- Healthy
- Degraded
- Unhealthy
- Unknown
- No health-bearing resources / informational-only

Do not make the popover appear only for unhealthy/problem resources.

Clicking the Application Health badge should open ONE consistent Application Health popover.

The popover must use ALREADY FETCHED DATA.
Do not introduce a separate fetch just to render the popover.

The only exception remains the already-agreed Application-local Pod fetch, which belongs in the Application Health hook/resource list, NOT in shared infrastructure.

================================================
POPOVER CONTENT — REQUIRED
================================================

Design the popover around this structure:

1. OVERALL APPLICATION HEALTH
--------------------------------
Show prominently:

Status:
- Healthy
- Degraded
- Unhealthy
- Unknown
- No health-bearing resources

Then show a short human-readable reason explaining WHY.

Examples:

Healthy:
"All evaluated health-bearing resources are healthy."

Degraded:
"Some health-bearing resources are not fully ready."

Unhealthy:
"One or more health-bearing resources have definitively failed."

Unknown:
"Health could not be determined because a health-bearing resource returned an API/RBAC error or lacks the expected status information."

Do not invent a reason when the actual data does not support one.

2. RESOURCE SUMMARY / COUNTS
--------------------------------
Show a compact summary of the application's resources.

The user specifically needs to understand:
- how many Deployments
- how many Pods
- how many StatefulSets
- how many DaemonSets
- how many ReplicaSets
- how many Jobs
- how many CronJobs
- how many PVCs

For each relevant health-bearing kind, show useful count/status information.

Examples:

Deployments
  2 total
  2 healthy
  Ready: 4/4

Pods
  5 total
  Ready: 5/5

StatefulSets
  1 total
  Ready: 3/3

DaemonSets
  1 total
  Ready: 3/3

ReplicaSets
  2 found
  1 active/owned
  do not double-count a ReplicaSet that is already represented by its owning Deployment

Jobs
  3 total
  2 succeeded
  1 failed

CronJobs
  2 total
  show meaningful last-job/last-run health information if already available from fetched data

PVCs
  2 total
  2 Bound
  or explicitly show Pending/Lost where applicable

Do NOT blindly show fields that do not exist for a resource kind.

3. HEALTH-BEARING RESOURCES
--------------------------------
Clearly separate resources that actually influence Application Health.

At minimum handle:

- Deployment
- StatefulSet
- DaemonSet
- ReplicaSet
- Pod
- Job
- CronJob
- PVC

For each health-bearing resource kind, show a concise status/count summary.

Where possible show:
- total count
- healthy/ready count
- desired count
- failed count
- pending count
- relevant reason

Use the actual Kubernetes status fields already fetched by the Application Health hook.

Do not invent status from names or from UI appearance.

4. SUPPORTING RESOURCES
--------------------------------
Show a separate "Supporting resources" section.

Supporting resources include the kinds already classified as supporting by the Application Health implementation, such as:
- Service
- Endpoints
- EndpointSlice
- HPA
- ResourceQuota where currently classified as supporting

These MUST NOT silently become health-bearing merely because they are displayed.

Show their existence/count in the popover.

Example:

Supporting
  Services: 2
  Endpoints: 2
  EndpointSlices: 4
  HPAs: 1

If there is useful already-fetched status information, show it.
Do not invent health semantics for resources that are currently supporting-only.

5. INFORMATIONAL RESOURCES
--------------------------------
Show a separate "Informational" section.

Examples:
- ConfigMaps
- Secrets
- Ingress
- NetworkPolicy
- Roles
- RoleBindings
- LimitRange
- other resources currently classified informational

Show counts/existence only.

IMPORTANT:
Informational resources MUST NOT affect Application Health.

An application containing only ConfigMaps/Secrets/Ingress/etc. must NOT incorrectly become:
- Healthy
- Unknown
- No Resources

because of the informational resources.

The UI should make the distinction clear:
"Informational resources do not affect application health."

6. PROBLEMS / DIAGNOSTICS
--------------------------------
If there are actual health problems, show a "Problems" or "Diagnostics" section.

Only put genuinely relevant problems here.

Examples:

Deployment
  Ready 2/3
  Reason: ...

Pod
  CrashLoopBackOff
  Pod: xyz
  Container: abc

Job
  Failed
  Job: xyz

PVC
  Lost
  PVC: xyz

RBAC/API error
  Unable to fetch Pod resources
  Reason: 403 Forbidden

Do NOT turn every resource into a problem merely because it is supporting/informational.

7. EMPTY / NO-HEALTH-BEARING CASE
--------------------------------
Handle this explicitly.

If resources exist but none are health-bearing:

DO NOT show:
"No resources"

DO NOT show:
"Unknown"

Instead show something like:

"No health-bearing resources"
"Resources exist in this application, but none currently provide an application health signal."

Then separately show:
- Supporting count
- Informational count

This distinction is important.

================================================
IMPORTANT HEALTH SEMANTICS
================================================

Preserve the health model already implemented.

The precedence is:

UNHEALTHY
→ DEGRADED
→ UNKNOWN
→ HEALTHY
→ NO HEALTH-BEARING RESOURCES

with CHECKING handled separately while the Application Health fetch is loading.

Do NOT change the shared getStatus implementation.

Do NOT make UNKNOWN mean "this resource kind has no evaluator".

UNKNOWN should represent an actual inability to determine health, such as:
- RBAC/API error on a health-bearing resource
- missing expected status information where the implementation defines that as unknown

A resource that is merely supporting/informational must NOT cause UNKNOWN.

================================================
DEDUPLICATION
================================================

Preserve the existing Application Health deduplication model.

Deployment → ReplicaSet → Pod

StatefulSet → Pod

DaemonSet → Pod

Job → Pod

CronJob → Job → Pod

Do not blindly count parent and child as independent health failures.

A child with ownerReferences pointing to a fetched parent should not cause duplicate application-level health tallying when the parent is authoritative.

However:
- standalone ReplicaSets must still be evaluated
- standalone Pods must still be evaluated
- the Resources tab may still display the resources
- the popover should explain counts without double-counting the same workload health signal

Do not change shared code to accomplish this.

================================================
POD REQUIREMENT
================================================

Pod is an important health-bearing resource.

Do NOT modify shared defaultApiResources just to add Pod.

Do NOT modify shared useKubeLists/useProjectItems.

Use the existing Application-local useApplicationHealth resource list mechanism.

It may reuse the shared defaultApiResources value WITHOUT modifying it and append Application-local resources such as:

Pod
Job
CronJob

as already designed.

The popover should then consume the already-fetched Pod data.

For Pods, show meaningful information such as:
- total Pods
- Ready/Not Ready count
- failed/pending count
- CrashLoopBackOff / ImagePullBackOff / waiting reason where available

Do not require a second fetch when the data is already available.

================================================
RESOURCE-TAB POPover
================================================

The same Application Health model must be reflected in ProjectResourcesTab.

Do not maintain another independent health calculation.

The Resources tab health cell must use the existing Application Health/resource evaluator.

If a row has a Health badge, clicking it should provide useful health information.

For the application-level badge, show the complete Application Health popover.

For an individual resource-row badge, show the relevant resource's details from already-fetched data, using the same evaluator semantics.

Do not recreate the health algorithm inside ProjectResourcesTab.

================================================
UI QUALITY
================================================

The current popover is too sparse and inconsistent.

Make it:
- compact but information-dense
- easy to scan
- clearly grouped
- consistent for Healthy/Degraded/Unhealthy/Unknown
- readable when there are many resources
- useful for diagnosis without opening every resource individually

Do not make a huge table.

Prefer sections such as:

Application Health
------------------
Healthy
All evaluated resources are healthy.

Workloads
---------
Deployments     2    Ready 4/4
StatefulSets    1    Ready 3/3
DaemonSets      1    Ready 3/3
Pods            5    Ready 5/5
Jobs            2    2 succeeded

Supporting
----------
Services        2
Endpoints       2
HPA             1

Informational
-------------
ConfigMaps      4
Secrets         3
Ingress         1

Problems
--------
None

For an unhealthy application, Problems should become prominent and explain the actual failing resources.

================================================
NO SHARED-CODE MODIFICATIONS
================================================

Before finishing, run:

git diff --stat
git diff -- <shared files>

Verify that the following remain untouched:
- getStatus
- getPodStatus
- getGraphNodeStatus
- useKubeLists
- useProjectItems
- registerProjectApiResource
- defaultApiResources
- ClusterStatusPopover
- resourceMap / graph files

If any shared file was modified accidentally, REVERT ONLY THAT SHARED-CODE MODIFICATION before finishing.

Do NOT revert the intended Applications-local implementation.

================================================
VALIDATION
================================================

Do NOT perform endless validation loops.

Run one focused validation pass:

1. TypeScript/typecheck
2. ESLint
3. relevant Application Health tests
4. relevant ProjectDetails/ProjectResourcesTab tests
5. inspect git diff
6. if the local app is already running, perform ONE visual check of the Application Health popover

Test at minimum:

- Healthy application
- Degraded application
- Unhealthy application
- Unknown due to health-bearing API/RBAC error
- No health-bearing resources
- informational-only application
- Deployment ready/desired count
- Pod ready/total count
- StatefulSet ready/desired
- DaemonSet ready/desired
- ReplicaSet dedup with Deployment
- standalone ReplicaSet
- failed Job
- CronJob/last Job information
- PVC Bound/Pending/Lost
- supporting resources
- informational resources
- multiple applications
- every Application Health badge can open its expected popover

IMPORTANT:
Do not claim visual validation passed if the app was not actually reloaded against the new code.

================================================
DEFINITION OF DONE
================================================

The task is DONE only when:

1. Every Application Health badge has consistent popover behaviour.
2. Healthy resources also have a useful popover.
3. Popover explains the overall Application Health.
4. Deployment counts and ready/desired values are visible.
5. Pod counts and ready/total values are visible.
6. StatefulSet/DaemonSet/ReplicaSet information is visible.
7. Job/CronJob/PVC information is represented correctly.
8. Supporting resources are shown separately.
9. Informational resources are shown separately.
10. Informational/supporting resources do not affect health.
11. Problems/diagnostics are shown only when genuinely applicable.
12. No-health-bearing-resource is distinct from Unknown and No Resources.
13. Existing deduplication semantics are preserved.
14. No second independent health engine is introduced.
15. No shared code is modified.
16. No new unnecessary API endpoint/fetch is introduced.
17. Existing tests remain green and new/updated popover tests cover the important cases.
18. The implementation remains Applications-local and surgical.

Do not stop after merely making the popover clickable.
Do not stop after showing only failing resources.
The goal is a real Application Health SUMMARY + DIAGNOSTIC popover.

After implementation, give me ONLY:
- files changed
- what was implemented
- tests/typecheck/lint result
- confirmation that shared code is unchanged
- any genuinely remaining limitation

Then STOP. Do not start another audit or another iteration automatically.
