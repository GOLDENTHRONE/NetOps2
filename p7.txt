Do not modify any code yet. Do not implement the tooltip yet. Do not propose a UI workaround.

I want a source-level verification of the existing Headlamp behavior before we make any implementation decision.

Trace the complete data flow for the Namespace LIST used by ProjectList.tsx:

1. Namespace.useList() / useKubeObjectList()
2. Per-cluster query creation
3. How each cluster's successful result is represented
4. How a failed cluster's result is represented
5. Exactly where failed-cluster information is discarded, if it is discarded
6. What ProjectList.tsx actually receives
7. What discoverProjectsFromNamespaces() actually receives

Do NOT assume React Query cache behavior. Verify it from the actual current source code.

Use the current source code and give me exact file/function/line references wherever possible.

Test this exact scenario conceptually against the implementation:

Cluster A → Namespace LIST succeeds → [ns-a]
Cluster B → Namespace LIST fails → API/HTTP error
Cluster C → Namespace LIST succeeds → [ns-c]

I need to know exactly what reaches ProjectList.tsx and exactly what reaches discoverProjectsFromNamespaces().

Answer these questions explicitly:

A. Can the current ProjectList distinguish these two states?

1. Cluster B successfully returned zero namespaces.
2. Cluster B's Namespace LIST request failed.

B. If the answer is NO, identify the exact function/line where that distinction is lost.

C. Does the existing return value already expose enough information, such as clusterResults, errors, isError, etc., to distinguish the failure without changing the underlying Kubernetes query behavior?

D. If the information is already available somewhere upstream but ProjectList only consumes items, identify the smallest safe boundary where we can expose that information downstream.

E. If an additive change is required, propose the MINIMUM SAFE architectural change.

Critical constraints:

- Do NOT change the existing Namespace LIST/query behavior.
- Do NOT change the meaning of the existing `items` array.
- Do NOT fabricate Namespace objects when an API request fails.
- Do NOT convert an API failure into a successful empty `[]` result.
- Do NOT infer cluster membership from application/project data.
- Do NOT assume that a missing namespace means that the namespace does not exist.
- Do NOT assume React Query keeps previous data unless verified from the actual configuration/source.
- Do NOT introduce a second Namespace API call just for the tooltip unless the source proves there is no safe existing data path.
- Do NOT modify ProjectList.tsx until the data-flow question is fully answered.
- Preserve all existing Headlamp behavior and existing consumers.
- Treat "successful empty list" and "API failure/unknown" as fundamentally different states.

The architectural principle I want to preserve is:

Successful empty result:
    Namespace API succeeded
    → namespace count is genuinely zero
    → state is KNOWN

Failed Namespace API:
    Namespace API failed
    → we do not know the namespace state
    → state is UNKNOWN

Do not collapse UNKNOWN into zero.

Finally, give me ONLY these outputs:

1. EXACT CURRENT BEHAVIOR
   Explain the current data flow from per-cluster Namespace LIST → aggregation → ProjectList → discoverProjectsFromNamespaces().

2. EXACT POINT OF INFORMATION LOSS
   If cluster-specific API failure information is lost before ProjectList, show exactly where and why.

3. MINIMUM SAFE CHANGE
   If a change is necessary, describe the smallest additive change that exposes "unknown because Namespace API failed" to the Projects/Application layer without breaking existing logic.

4. TOOLTIP IMPLICATION
   Explain what states the tooltip can safely represent based ONLY on information that is actually available from the verified data flow.

Do NOT implement anything yet. I want the architectural decision first.
