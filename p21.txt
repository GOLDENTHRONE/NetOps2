===============================================================================
 p21.txt — P0 LEFTOVER VERIFICATION (finish what p20 couldn't prove), rigorous
 Branch: GT_D_V1   |   App: http://localhost:3000   |   Live OCP cluster
 Why this exists: the p20 run left TWO things unproven —
   (L1) the real 401/403 OPEN-GATE path: the logout produced a NETWORK 502, not a
        server 401/403, so "401 -> gate immediately" was NOT actually proven.
   (L2) RECOVERY LATENCY after re-login was never measured (row was already Ready
        before timing started).
 This prompt finishes BOTH, on EVERY page, with clicks, using the Network tab as
 un-fakeable source of truth. Be critical and honest: if you cannot produce a real
 401/403, say so and mark it UNPROVEN — do not claim a pass.
 Audience: an executing agent driving the browser + a human who logs in/out on cue.
===============================================================================

-------------------------------------------------------------------------------
 0) PRECONDITIONS (verify BEFORE testing — do not skip)
-------------------------------------------------------------------------------
 0.1 The p20 PRE-REQUISITE fix must be present in the working tree: open
     frontend/src/lib/k8s/index.ts -> useClustersVersion queryFn catch must read:
        const status = (err as ApiError)?.status;
        consecutiveFailuresRef.current[clusterName] =
          status === 401 || status === 403 ? 0
            : (consecutiveFailuresRef.current[clusterName] ?? 0) + 1;
     If it is NOT there, STOP and apply it first (see p20). L2 depends on it.
 0.2 The earlier temporary logs (p0log / gtDebug) should already be REMOVED. Confirm:
        grep -rn "p0log\|p0test\|gtDebug" frontend/src   -> ZERO matches.
     This run ADDS its OWN temp logs (Section 1B) to make the flow + every response
     visible, AND uses the Network tab as the un-fakeable cross-check. Both the code
     logs and any timing log are removed again in STEP 6 (logs only, nothing else).

-------------------------------------------------------------------------------
 1) GROUND RULES (critical & honest)
-------------------------------------------------------------------------------
 1.1 Do NOT change P0 logic. Only observe (optionally add ONE timing log, removed after).
 1.2 SOURCE OF TRUTH = the DevTools NETWORK tab. It is the browser's own record of
     the real request + real HTTP status; it cannot be faked. Every claim below must
     cite the actual request path + Status you saw in Network. "It looked fine" is
     not evidence — the Network row is. The code logs you add in Section 1B are a
     SECOND view of the same flow — each api.response log MUST match the Network
     Status for that request; if they differ, the log is wrong (fix it, don't trust it).
 1.3 The #1 mistake last time: using DevTools "Offline" (or killing the network) to
     "log out" produces a NETWORK failure (shows as 502/failed), NOT a server
     401/403. That does NOT prove the auth gate. For L1 you MUST get a REAL 401/403
     FROM THE SERVER while the network is UP (see STEP 2).
 1.4 If a real 401/403 cannot be produced, mark L1 UNPROVEN and say why. Honesty > a
     green checkmark.

-------------------------------------------------------------------------------
 1B) ADD TEMP FLOW + RESPONSE LOGS (gated; REAL values only; removed in STEP 6)
-------------------------------------------------------------------------------
 Add lightweight logs so the whole P0 flow and EVERY response are visible and can be
 cross-checked against Network. Gate them so they are silent unless enabled.
 1B.1 New file frontend/src/lib/p21log.ts:
      export function p21log(scope: string, data: Record<string, unknown>): void {
        try {
          const w = window as unknown as { __P21__?: boolean };
          if (typeof window === 'undefined' || !w.__P21__) return;
          // eslint-disable-next-line no-console
          console.log(`[P21 ${new Date().toISOString().slice(11,23)}] ${scope}`, data);
        } catch { /* never break the app */ }
      }
 1B.2 EVERY API RESPONSE — clusterRequests.ts, right AFTER `response = await fetch(...)`
      settles (before `if (!response.ok)`) — print the REAL status (anti-fake anchor):
        p21log('api.response', { cluster, path, method:(opts.method as string)??'GET',
          status: response.status, ok: response.ok });
      and inside the !ok block: p21log('api.error', { cluster, path, status, statusText });
 1B.3 TABLE version decision — index.ts useClustersVersion (after lastStatusError is
      finalized in the forEach):
        p21log('table.version', { cluster: clusterName,
          rawErrorStatus:(error as ApiError|null)?.status??null, hasData:data!==undefined,
          consecutiveFailures:consecutiveFailuresRef.current[clusterName]??0,
          shownErrorStatus:lastStatusError?.status??null });
 1B.4 AUTH poll decision — index.ts useClustersAuth (in its map forEach):
        p21log('auth.poll', { cluster:clusterName,
          rawErrorStatus:(results[i].error as ApiError|null)?.status??null,
          shownErrorStatus:last?.status??null });
 1B.5 OPEN-GATE decision — RouteSwitcher.tsx AuthRoute getRenderer:
        success branch -> p21log('authgate', { cluster, screen:'page' });
        keep-last-good branch -> p21log('authgate', { cluster, screen:'keep-last-good+chip', status });
        gate branch -> p21log('authgate', { cluster, screen:'gate', gateState, status });
      retry fn -> p21log('authgate.retry', { status:error?.status, failureCount });
 1B.6 ROW text — ClusterTable.tsx getPlainStatusText (before return):
        p21log('row.status', { cluster:cluster?.name,
          versionErrorStatus:errors[cluster?.name]?.status??null, text });
 Add the correct relative import of p21log to each edited file. Then:
   npx tsgo --noEmit   (touched files clean)   and   npx eslint --fix <touched files>.
 Logs print only status codes / booleans / cluster names / status text — NEVER token
 or header values. Enable at runtime (Section 3) with: window.__P21__ = true .

-------------------------------------------------------------------------------
 2) HOW TO PRODUCE A REAL 401/403 (network UP, credential INVALID) — the crux
-------------------------------------------------------------------------------
 The network must stay ONLINE; only the CREDENTIAL must become invalid so the SERVER
 answers 401 (unauthenticated) or 403 (unauthorized). Pick whichever you can do:
   (a) 401: ASK HUMAN to invalidate the session server-side while online — e.g.
       `oc logout` in a terminal (revokes the token), or let the OCP token expire,
       or clear the app's auth cookie via DevTools > Application > Cookies (delete
       the auth cookie) and let the next poll hit the server. Do NOT go Offline.
   (b) 403: ASK HUMAN to use a login with limited RBAC / a namespace they lack
       rights on, so selfsubjectrulesreviews or a resource call returns 403.
 CONFIRM IT IS REAL: in the Network tab the request must show Status 401 (or 403)
 with a response body (a k8s Status object), NOT "(failed)"/"(blocked)"/pending and
 NOT 502/0. If you only ever see 502/failed, you are still causing a network error —
 fix that before claiming L1.

-------------------------------------------------------------------------------
 3) SETUP
-------------------------------------------------------------------------------
 3.1 cd frontend && npm run start . Open http://localhost:3000/ (All Clusters).
 3.2 DevTools (F12) -> NETWORK: "Preserve log" ON. Filter:
        version|selfsubjectrulesreviews|clusterversions|/c/
     Show the Status column. Keep a clock/stopwatch ready for L2.
 3.3 CONSOLE: enable the Section-1B logs ->  window.__P21__ = true  . Filter box:  [P21
     (no closing bracket — the tag is "[P21 <time>] ..."). Keep console visible to
     catch red errors on each page too.
 3.4 Note the OCP <cluster> name from a request URL.

===============================================================================
 4) THE TEST — every page, with clicks (GO -> CLICK -> LOOK -> VERIFY in Network)
===============================================================================

--- STEP 1 — Healthy baseline sweep (prove the starting point on every page) ----
 GO:    http://localhost:3000/  (PAGE-1 All Clusters).
 LOOK:  cluster row = "Ready" (or Active/Reachable).
 CLICK: status cell -> PAGE-2 popover -> note interval (~10s), last/next check. Close.
 CLICK: cluster NAME -> PAGE-3 /c/<cluster> -> cluster overview loads.
 CLICK (sidebar): Workloads -> Pods -> Namespaces -> Nodes -> open ONE resource
        detail (click a Pod/Deployment row). EACH must load with data.
 CLICK: "Applications" (/projects) then back to "All Clusters" (PAGE-4 sanity).
 VERIFY (Network): /version=200 ; selfsubjectrulesreviews=200/201 ; resource list
        calls on inner pages =200. No red console errors on ANY page.
 PASS = every page healthy before we break anything.

--- STEP 2 (L1) — REAL 401/403 must GATE immediately, on every page --------------
 Precondition: you are logged in and on PAGE-3b (an inner page, e.g.
   http://localhost:3000/c/<cluster>/pods). Network ONLINE.
 DO: produce a REAL 401/403 per STEP 2 above (oc logout / token expiry / delete auth
   cookie / limited-RBAC). Keep the network ONLINE. Reply-gate with the human.
 VERIFY IN NETWORK FIRST: the cluster's request (auth-route /c/<cluster>... or
   selfsubjectrulesreviews or /version) shows a real Status 401 (or 403) — NOT 502,
   NOT (failed). If it is 502/failed, STOP — you have a network error, not an auth
   error; redo STEP 2 so the SERVER returns 401/403.
 THEN check the UI on EACH page (navigate + click):
   - PAGE-3b inner page you are on: the centered ACCESS GATE appears IMMEDIATELY
     (401 -> "expired"/sign-in gate; 403 -> forbidden/permissions). No stale
     resource list is left showing.
   - CLICK other sidebar links (Workloads, Namespaces, Nodes): each also shows the
     gate immediately — you cannot browse resources while unauthorized.
   - GO to PAGE-1 (All Clusters): the row shows "Authentication required" (401) or
     "Insufficient permissions" (403), immediately (not kept-last-good, not debounced).
   - CLICK the status cell -> PAGE-2 popover: reflects the same failure.
 VERIFY (Network) for each: the underlying call really returned 401/403.
 PASS (L1) = a REAL server 401/403 gates the open-cluster pages at once AND flips the
   table row at once, on every page. If you only got 502, mark L1 UNPROVEN + why.

--- STEP 3 (L2) — RECOVERY LATENCY after re-login, MEASURED, both paths ----------
 Goal: after re-login, how many SECONDS until the status returns to Ready, WITHOUT a
 reload — measured, for BOTH the auth-probe path AND the /version path.
 Prep: keep the tab FOCUSED (polling pauses when hidden). Have the stopwatch ready.
   Use the Section-1B logs for precise timestamps: the [P21] row.status line flips to
   text:"Ready" and the [P21] api.response line shows status:200/201 — read the
   timestamps in those log lines (and cross-check the Network request timestamps).
 DO: ASK HUMAN: "Please LOG BACK IN now, and tell me the exact moment you're done."
   START the stopwatch when they say done. Do NOT reload.
 MEASURE:
   - t_authprobe = seconds from "logged in" until selfsubjectrulesreviews returns
     200/201 in Network AND the row shows "Ready". EXPECT ~10s (<=15s), no reload.
   - t_version = if a cluster's /version itself was 401/403, seconds until /version
     returns 200 in Network AND that row leaves "Authentication required". EXPECT
     ~10s (<=15s) now that the p20 useClustersVersion fix is in — this is the PROOF
     the fix works. If it takes ~30-60s, the fix did NOT take effect (recheck 0.1).
 CHECK the UI recovers on EVERY page: PAGE-1 row -> Ready ; open PAGE-3 (click name)
   -> cluster overview (gate gone) ; PAGE-3b inner pages (Pods/Namespaces) load data
   again ; PAGE-2 popover shows a fresh recent check.
 PASS (L2) = both paths recover within ~1 poll (~10-15s), no reload, on every page.

--- STEP 4 — Re-confirm the p20 basics didn't regress (quick) --------------------
 (These already passed in p20 but re-confirm now that the version fix is in.)
 4a: one Offline blip (~3s) on PAGE-1 -> row STAYS "Ready"/"Active" (keep-last-good),
     Network shows one 502/408, row does not flip. Then online.
 4b: stay Offline ~25-30s -> row turns "Unavailable" only after >=2 failures.
 4c: Slow-3G (~6-10s latency) -> /version eventually 200, row NOT falsely Unavailable
     (proves the 15s timeout tolerance). Back to No throttling.

===============================================================================
 5) RESULTS (fill with REAL Network values — no guessing)
===============================================================================
 Cluster: __________
 Precondition 0.1 version fix present? YES/NO   0.2 logs already zero? YES/NO
 STEP1 healthy sweep (all pages) ...... PASS/FAIL   any page with errors? ______
 STEP2 (L1) real 401/403 gate:
     Network status actually seen: ______ (MUST be 401/403, not 502)
     PAGE-3b inner gate immediate? ____  other inner links gate? ____
     PAGE-1 row text: ______  PAGE-2 popover ok? ____
     VERDICT L1: PASS / UNPROVEN (reason: __________)
 STEP3 (L2) recovery latency:
     t_authprobe = ____ s (want ~10-15s, no reload)
     t_version   = ____ s (want ~10-15s; if ~30-60s the fix didn't take)
     recovered on all pages? ____  reload needed? Y/N
     VERDICT L2: PASS / FAIL
 STEP4 regression: 4a __ 4b __ 4c __
 Red console/DOM errors anywhere? ______________________

===============================================================================
 6) CLEANUP + HANDOFF (logs only; do NOT commit/push without the human)
===============================================================================
 6.1 Remove ALL Section-1B logs (logs only — nothing else):
     - Delete frontend/src/lib/p21log.ts
     - Remove every p21log(...) call and every `import { p21log } ...` line from
       clusterRequests.ts, index.ts, RouteSwitcher.tsx, ClusterTable.tsx (and any
       other file you touched).
     - Confirm:  grep -rn "p21log\|p21log\.ts\|\[P21\]\|p0log\|p0test\|gtDebug" frontend/src
       -> ZERO matches.
 6.2 CRITICAL diff review — `git diff`/`git status`: after log removal, the ONLY code
     change that should remain is the p20 useClustersVersion fix + its regression test
     (staged from the p20 run). NO p21 logging may survive, and NO other line may
     change. Read EVERY hunk: each removed line must be a log call / log import /
     log-only comment; if any hunk alters real behavior, revert THAT hunk. Where a log
     used a variable introduced only for it (e.g. a `text` local), ensure the original
     return value is unchanged. Net effect of p21 on the code = ZERO (only the p20 fix
     remains).
 6.3 Validate: npx eslint <touched files> clean ; npx vitest run
     src/lib/resilience.test.ts src/components/App/RouteSwitcher.authgate.test.tsx
     src/lib/k8s/index.test.ts  (the ~12 known pre-existing index.test failures may
     remain; the count must NOT increase) ; the repo build succeeds.
 6.4 Print `git status` + `git diff --stat`. Do NOT commit or push unless the human
     explicitly says so.

-------------------------------------------------------------------------------
 NOTES (honest)
-------------------------------------------------------------------------------
 - The ONLY acceptable proof for L1 is a Network row showing a real 401/403 from the
   server. A 502/failed is a network error and proves nothing about the auth gate.
 - L2 is the live proof that BOTH the auth fix (aec158d) and the p20 /version fix
   recover in ~10s. A ~60s /version recovery means the /version fix is missing.
 - Labels: 401 -> "Authentication required" ; 403 -> "Insufficient permissions" ;
   timeout/5xx (>=2) -> "Unavailable" ; ok -> Ready/Reachable/Active.
===============================================================================
 END p21.txt
===============================================================================
