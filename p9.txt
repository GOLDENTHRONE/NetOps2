STOP. Do not modify, revert, delete, refactor, or implement anything.

I need an impact audit of the CURRENT working tree.

The intended scope of this feature is STRICTLY:
Application tab → existing Health column → Health popover showing evidence for the SAME health status.

I do NOT want any changes to:
- multi-cluster behavior
- Namespace behavior
- Application discovery
- Kubernetes query semantics
- shared resource status behavior
- unrelated utilities

You previously identified 11 changed files, including files that may be outside the tooltip/popover scope.

For EVERY currently modified file:

1. Show the exact git diff for that file.
2. Explain what behavior the change actually modifies.
3. Identify whether the change is:
   A. REQUIRED for the Application Health popover
   B. REQUIRED only because of the current health-evaluation implementation
   C. OUT OF SCOPE and should not be part of this feature
   D. TEST-ONLY
4. Identify what other existing Headlamp UI/features could be affected by the change.
5. Do NOT make any changes.

Pay special attention to:
- KubeObjectStatus.tsx
- utils
- projectHealthEvaluators.ts
- useKubeLists.ts
- projectUtils.ts
- useProjectResources.ts

Do not infer impact from filenames. Inspect the actual diff and usages/references in the repository.

At the end produce:

| File | Actual change | Why it was changed | Needed for popover? | Other behavior affected? | Recommendation |
|------|---------------|--------------------|---------------------|--------------------------|----------------|

Then give me:
1. Changes that are definitely required.
2. Changes that are definitely out of scope.
3. Changes whose necessity cannot yet be proven.
4. Any risk to existing Headlamp behavior.

DO NOT MODIFY CODE.
