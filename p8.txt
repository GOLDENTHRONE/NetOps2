I want a CURRENT-STATE AUDIT of the Application Health column and its Health popover.

IMPORTANT SCOPE:
This task is ONLY about the Application tab's Health column and the Health popover/evidence.

Do NOT investigate or modify:
- multi-cluster behavior
- cluster discovery
- Namespace discovery behavior
- Application discovery behavior
- how Applications are added/removed from the list
- existing Headlamp resource-fetching architecture
- unrelated UI
- unrelated refactoring

Do NOT change any code.

I need you to inspect the CURRENT working tree/source code, not rely on previous analysis, previous screenshots, or assumptions.

My product requirement is:

When a user opens the Application tab, every Application already rendered there has a Health value.

For example:
- Healthy
- Degraded
- Unhealthy
- Progressing
- Unknown, if currently supported

When the user clicks the Health value, a popover should explain:

"WHY does this Application have this exact Health status?"

The evidence shown in the popover must be derived from the SAME health evaluation/data that produced the Health status.

The popover must NOT independently calculate a different health result.

FIRST: AUDIT THE CURRENT IMPLEMENTATION.

Trace the CURRENT code from:

Application row
→ Health column
→ health calculation/evaluation
→ displayed status/icon/text
→ Health cell click/trigger
→ current popover component
→ current popover content

I need exact file paths, functions/components, and line numbers.

Answer these questions:

1. What exact code currently determines the Application Health status?

2. What exact statuses can the current implementation produce?

3. For each status, what exact conditions/data produce it?

4. What resource kinds currently participate in Application Health?

5. What resource kinds are fetched but DO NOT participate in Health?

6. For every health-bearing resource kind, identify the exact fields used as evidence.

Examples only — verify, don't assume:
- Deployment desired/ready replicas
- Deployment conditions
- StatefulSet desired/ready replicas
- DaemonSet readiness
- Pod phase
- Pod Ready condition
- container waiting/terminated reasons
- Job failures
- PVC state

7. Does the current health calculation distinguish:
   - Healthy
   - Degraded
   - Unhealthy
   - Progressing
   - Unknown
   - No Resources
   ?
   If not, tell me exactly what it currently does.

8. What happens when there are zero resources?

9. What happens when a resource has missing status fields?

10. What happens when a resource API request fails?

11. What happens while the health data is still loading?

12. What happens if only some health-bearing resources are available?

13. Is there currently a Health popover?
   If yes:
   - exact component
   - exact trigger
   - exact content
   - exact data source
   - whether it is actually connected to the Application Health result

14. If there is NO popover yet, explicitly say so.

15. If there IS a popover but it does not yet show evidence, explicitly identify what is missing.

MOST IMPORTANT:

Build a table like this from the CURRENT CODE:

| Application Health | Exact condition that produces it | Evidence currently available | Evidence currently shown in popover |
|--------------------|-----------------------------------|------------------------------|-------------------------------------|
| Healthy            | ...                               | ...                          | ...                                 |
| Degraded           | ...                               | ...                          | ...                                 |
| Unhealthy          | ...                               | ...                          | ...                                 |
| Progressing        | ...                               | ...                          | ...                                 |
| Unknown             | ...                               | ...                          | ...                                 |
| No Resources       | ...                               | ...                          | ...                                 |

Do not fill anything by assumption. If a status does not exist, say "NOT IMPLEMENTED".

Then answer:

A. What is already correctly implemented?

B. What is currently wrong or incomplete specifically for the Health/evidence requirement?

C. What evidence is already available in the existing health calculation but is not exposed to the UI?

D. What evidence would have to be added because it does not currently exist?

E. What is the MINIMUM code change needed to make the popover explain the SAME health decision that the Health column displays?

CRITICAL ARCHITECTURAL REQUIREMENT:

The final design must have ONE source of truth.

Conceptually:

ApplicationHealthEvaluation
        ↓
   ┌────┴────┐
   ↓         ↓
Health     Popover
column     evidence

The popover must consume the evaluation/evidence produced by the health calculation.

Do NOT create a second independent health calculation inside the popover.

Do NOT modify the existing Application discovery or cluster behavior.

Do NOT implement anything yet.

At the end, give me a concise "CURRENT STATE" summary containing only verified facts from the current code.
