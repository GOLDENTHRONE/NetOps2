STOP proposing implementation changes for now.

We have established an important product invariant:

AN APPLICATION BELONGS TO EXACTLY ONE CLUSTER.

The Application name always contains/prefixes the cluster name, so an Application cannot legitimately belong to two clusters.

I want you to investigate the current source code and determine exactly where our reasoning is wrong.

Do NOT modify any files.

Answer these questions with exact file names, function names, and relevant code/data flow:

1. Find where Application identity/name is created, parsed, or derived.
   - Show the exact function and file.
   - Explain the naming convention.
   - Give an example using the real code.

2. Verify whether the cluster name is deterministically encoded in the Application name.
   - If yes, explain exactly how the cluster can be identified from an Application name.
   - If no, explain what the actual invariant is.

3. Trace how ProjectDefinition is created.
   - What inputs are required?
   - Why is Namespace discovery currently required before an Application row can exist?
   - Identify the exact point where Application identity is lost when Namespace LIST fails.

4. Trace the complete Applications-tab data flow:

   cluster
     -> namespace discovery
     -> ProjectDefinition
     -> application identity
     -> resource queries
     -> health evaluation
     -> rendered Application row

   Identify every place where failure of Namespace LIST causes an Application to disappear.

5. Search the repository for ANY existing authoritative source of Application names/identities that does NOT depend on the current Namespace LIST.
   Include:
   - application name construction
   - cluster/application mappings
   - cached application data
   - ArgoCD Application objects
   - backend APIs
   - React Query caches
   - localStorage/Redux
   - any existing cluster-level inventory

6. Most importantly:
   Given the invariant that one Application belongs to exactly one cluster, determine whether we actually need:

      cluster -> namespace -> application

   OR whether the existing system can/should establish:

      cluster -> application

   directly.

7. Tell me which statement is TRUE:

   A. We genuinely cannot know the Application identity when Namespace discovery fails.

   B. We can know the Application identity from an existing authoritative source, but the current code unnecessarily depends on Namespace discovery.

   C. We can derive the Application identity from the cluster/Application naming convention.

   D. Some combination of B and C.

8. Explain the MINIMUM architectural change required to preserve the Application row when Namespace discovery fails.

9. Do NOT recommend persisted/last-known inventory, allowedNamespaces fallback, or synthetic Application names unless the source code proves they are necessary.

10. End with a concise section:

   "THE ACTUAL BUG"

   Explain in plain English what the current implementation assumes, why that assumption is wrong, and what data it should use instead.

This is a READ-ONLY investigation. Do not edit code.
