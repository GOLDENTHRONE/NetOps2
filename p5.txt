Before making ANY code changes, inspect the current Headlamp repository and verify the actual data flow for Application Health / Project Health.

I do NOT want a proposed implementation yet. I want a factual verification based on the current code.

Please trace the complete runtime flow and answer these questions with exact file names, functions/hooks, and relevant code references:

1. APPLICATION SCOPE
- How does a Project/Application determine which clusters and namespaces belong to it?
- Is namespace information already available/configured before resource fetching starts?
- Can an application/project exist with a known namespace but without successful cluster-wide namespace discovery?
- Are namespaces fetched dynamically, or are they already part of the application/project configuration?

2. NAMESPACE AVAILABILITY
- Find the exact code responsible for namespace discovery/listing.
- What happens if namespace discovery succeeds?
- What happens if namespace discovery fails?
- Is a namespace object still available from another source/configuration when namespace listing fails?
- Is there already any cached, persisted, configured, or last-known namespace information?
- Do NOT assume it exists — verify from code.

3. RESOURCE API AVAILABILITY
For the resources currently used by Application Health, verify for each:
- Pods
- Deployments
- StatefulSets
- DaemonSets
- Jobs
- PVCs
- any supporting resources

For each resource type:
- Where is it fetched?
- Is the request namespace-scoped or cluster-scoped?
- What does the hook return on loading?
- What does it return on success?
- What does it return on error?
- Can we distinguish "zero resources" from "API request failed"?
- Is truncation/limit information available?
- Is the cluster identity available with the observation?

4. RBAC / PERMISSION SCENARIOS
Verify what happens if:
- the user can access a configured namespace but cannot list all namespaces;
- the namespace exists but resource listing is forbidden;
- namespace discovery is forbidden;
- a resource API returns an error;
- the resource list returns zero items.

Do not infer. Trace the actual Headlamp/Kubernetes client behavior in this repository.

5. CURRENT APPLICATION HEALTH IMPLEMENTATION
Trace:
Application/Project
→ project resources hook
→ resource observations
→ health evaluator
→ UI row

Identify exactly where the current implementation gets:
- cluster
- namespace
- resource kind
- loading
- success
- error
- truncated
- resource status

6. THE CURRENT P0 BLOCKER
The current note says:

"Namespace discovery failure for a cluster-only application still cannot render an Unknown row, because current application rows are created only from successfully fetched Namespace data."

Verify whether this statement is actually true in the current code.

Specifically answer:
A. Can a cluster-only application have enough authoritative information to render an Unknown row even when namespace discovery fails?
B. If yes, where does that information come from?
C. If no, what information is genuinely missing?
D. Is a last-known/persisted namespace inventory actually necessary?
E. Would a cluster-level Unknown row be semantically correct?
F. Or is there a simpler solution using information that already exists in the application/project configuration?

7. IMPORTANT
Do NOT change any files.
Do NOT implement a solution.
Do NOT invent APIs or behavior.

At the end provide:

- VERIFIED FACTS
- CURRENT DATA FLOW
- WHAT IS AVAILABLE WHEN NAMESPACE DISCOVERY FAILS
- WHAT IS NOT AVAILABLE
- WHETHER THE CURRENT P0 BLOCKER IS REAL
- MINIMUM CHANGE NEEDED (only conceptually, no code)
- Any assumptions that still need to be validated manually

Include exact file paths and function/hook names for every important conclusion.
